# Future work

The following questions were deliberately left outside the current preliminary analysis:

- Define the separate live-damaged plus dead-tree severity metric with Joan and Meng before implementing it.
- Compare previous-condition, current-condition, and condition-change-matrix approaches if stable numeric CONDID is later relaxed.
- Evaluate alternative mortality annualization or denominator definitions only as sensitivity analyses; the current model uses cumulative mortality.
- Extend the completed seedling subplot-number sensitivity to exact microplot footprints if spatially explicit confidential FIA geometry becomes available.
- Add topography, soil, forest type, climate normals, and other controls when the final model structure is agreed.
- Decide whether the final analysis should use a causal-inference framework.
- Evaluate nonlinear terms, interactions, spatial structure, and alternative uncertainty estimators after the core data definitions are frozen.
